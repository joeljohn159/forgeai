// ============================================================
// forge map — Git visualization (v0.2)
// ============================================================

import chalk from "chalk";

export async function mapCommand() {
  console.log(chalk.bold("\n🗺️  Forge Map\n"));
  console.log(chalk.yellow("  Coming in v0.2 — Git branch visualization in browser.\n"));
  console.log(chalk.dim("  For now, use: forge status\n"));
}
